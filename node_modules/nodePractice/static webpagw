const http = require('http')
const url = require('url')
const fs = require('fs')
let server = http.createServer((req, resp) => {
    const path = req.url
    if (path === "/home" || path === "/index.html" || path === "/home.html")
        resp.setHeader("content-type", "text/html")

    let home = fs.readFileSync()
})