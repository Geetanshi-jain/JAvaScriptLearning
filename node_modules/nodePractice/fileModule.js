var fs = require('fs');
console.log('Before Reading.......')

fs.readFile('os.js', 'UTF-8', (err, data) => {
    if (err)
        console.log(err);
    else
        console.log(data);
});
-*
console.log("After reading");

fs.readdir('nodePractice', 'UTF-8', (err, data) => {
    if (err)
        console.log(err);
    else
        console.log(data);
});

let dataToWrite = 'my self Geetanshi jain attendant of IIps Davv indore';

const code = `console.log("hello world")
console.log("Hello js")`;

fs.writeFile('nodePractice/os.js', code, (err) => {
    if (err)
        console.log(err);
    else
        console.log('File written successfully');
});
